marketplace:
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://localhost:11434}

springdoc:
  swagger-ui:
    enabled: true

logging:
  level:
    com.enterprise.marketplace: DEBUG

management:
  endpoint:
    health:
      show-details: always
