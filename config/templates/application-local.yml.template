spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    enabled: true

logging:
  level:
    com.enterprise.marketplace: DEBUG
    org.springframework.web: DEBUG

management:
  endpoint:
    health:
      show-details: always
