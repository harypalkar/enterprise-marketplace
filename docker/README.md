# Docker configurations will be added in Milestone 2 (Infrastructure).
#
# Planned contents:
# - Dockerfile per microservice (multi-stage, Java 21 JRE)
# - docker-compose.yml (PostgreSQL, Redis, Kafka, Elasticsearch, Keycloak, etc.)
# - .env.example for local environment variables
