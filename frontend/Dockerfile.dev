FROM node:24-slim

WORKDIR /app

EXPOSE 5173

ENV VITE_GATEWAY_URL=http://host.docker.internal:9080

# Source is mounted at runtime; install deps then start Vite.
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 5173"]
