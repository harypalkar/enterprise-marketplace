package com.enterprise.marketplace.notificationservice.enums;

public enum NotificationStatus {
    PENDING,
    QUEUED,
    PROCESSING,
    SENT,
    DELIVERED,
    FAILED,
    RETRY,
    CANCELLED
}
